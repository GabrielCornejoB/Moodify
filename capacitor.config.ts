import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moodify.myapp',
  appName: 'moodify',
  webDir: 'dist/moodify',
  bundledWebRuntime: false
};

export default config;
